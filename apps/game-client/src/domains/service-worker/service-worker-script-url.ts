/** URL where the compiled service worker is served (and where the page registers it: navigator.serviceWorker.register(SERVICE_WORKER_SCRIPT_URL)). */
export const SERVICE_WORKER_SCRIPT_URL = '/serwist/sw.js';

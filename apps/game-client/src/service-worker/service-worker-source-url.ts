/** Path to the service worker source file. Used by the Serwist route (serwist/[path]/route.ts) as swSrc so it can compile and serve the SW. */
export const SERVICE_WORKER_SOURCE_URL = 'src/domains/service-worker/service-worker.ts';
